import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final_ing_aplicada_pwa',
  webDir: 'dist'
};

export default config;
